# `packages/` Directory

This directory is reserved for shared packages, utilities, and libraries that can be consumed by apps in `apps/`.

Example future packages:
- `packages/ui` — Shared UI component library
- `packages/config` — Shared ESLint/Tailwind configs
- `packages/utils` — Common utility functions
